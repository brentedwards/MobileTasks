<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
						 xmlns:converters="clr-namespace:MobileTasks.XForms.Converters"
             x:Class="MobileTasks.XForms.Views.Tasks"
						 BackgroundColor="White">
	<ContentPage.Resources>
		<ResourceDictionary>
			<converters:TaskImageSourceConverter
				x:Key="TaskImageSourceConverter"
				CompletedSource="IconCompleted.png"
				IncompleteSource="IconIncomplete.png"
				PastDueSource="IconPastDue.png" />
		</ResourceDictionary>
	</ContentPage.Resources>
	<ContentPage.ToolbarItems>
		<ToolbarItem Text="Add" x:Name="Add" />
	</ContentPage.ToolbarItems>
	<ListView x:Name="TaskList" ItemsSource="{Binding Tasks}">
		<ListView.ItemTemplate>
			<DataTemplate>
				<ViewCell>
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition />
						</Grid.ColumnDefinitions>

						<!--<Switch IsToggled="{Binding IsCompleted}" />-->
						<Image Source="{Binding Converter={StaticResource TaskImageSourceConverter}}" HorizontalOptions="CenterAndExpand">
							<Image.GestureRecognizers>
								<TapGestureRecognizer Command="{Binding UpdateTaskCommand}" CommandParameter="{Binding}" />
							</Image.GestureRecognizers>
						</Image>

						<Grid Grid.Column="1">
							<Grid.RowDefinitions>
								<RowDefinition />
								<RowDefinition />
							</Grid.RowDefinitions>

							<Label Text="{Binding Description}" FontSize="20" TextColor="Black" />

							<Label Text="{Binding DateDue}" Grid.Row="1" FontSize="16" TextColor="Gray" />
						</Grid>
					</Grid>
				</ViewCell>
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</ContentPage>